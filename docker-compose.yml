services:
  bot:
    build: .
    container_name: claude-bot
    restart: unless-stopped
    ports:
      - "${DASHBOARD_PORT:-8080}:8080"
    volumes:
      # Persist session database across restarts
      - ./data:/app/data
      # Mount projects directory (read-write — agents need to create/edit files)
      - ${CLAUDE_PROJECTS_DIR:-~/claude-projects}:/projects
    env_file:
      - .env
    environment:
      - CLAUDE_PROJECTS_DIR=/projects
      - CONVERSATION_STORE_DIR=/app/data
      # Pass ANTHROPIC_API_KEY from host .env to container
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/api/stats"]
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: "2.0"
        reservations:
          memory: 512m
          cpus: "0.5"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
